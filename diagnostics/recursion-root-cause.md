Notebook 04 reloads `native_state_packet`. That module captured `grouped._infer_optimizer_shape_groups` into `_ORIGINAL_INFER` and then monkey-patched the grouped module. On reload, `_ORIGINAL_INFER` captured the prior packet wrapper; because both generations share the reloaded module globals, fallback called itself recursively. The compatibility behavior must be implemented without module-global monkey-patching.
